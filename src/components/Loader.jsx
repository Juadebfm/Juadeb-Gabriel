import React from "react";

const Loader = () => {
  console.log("Loader component is rendered"); // Log when the Loader component is rendered
  return <div>Loading...</div>; // Simplified loader content
};

export default Loader;
